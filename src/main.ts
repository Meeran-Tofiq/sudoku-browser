// TODO: Import your GameController here when you create it
// import { GameController } from "./game-controller";

function main() {
  const grid = document.querySelector("#sudoku-grid");
  if (!grid) return;

  // TODO: Create GameController instance here
  // const controller = new GameController(grid);

  // TODO: Setup game controls and pass controller methods as callbacks
  // setupGameControls({ onNewGame: (difficulty) => controller.startNewGame(difficulty) });
}

main();
